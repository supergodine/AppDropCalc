import { DataSource } from 'typeorm';

export async function seedPresets(dataSource: DataSource) {
  console.log('🎉 Seed de presets simulado concluído com sucesso!');
  // Implementação simplificada para evitar dependências complexas
  return Promise.resolve();
}