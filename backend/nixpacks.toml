
# Railway/Nixpacks: Force install all dependencies for NestJS build
[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/main.js"
